cmake_minimum_required(VERSION 2.8.3)
project(landshark_cmu_track_pos)

<<<<<<< HEAD
find_package(catkin REQUIRED COMPONENTS
 actionlib_msgs
 actionlib
 landshark_msgs
 landshark_common
 tf
 geometry_msgs
 landshark_gps
 nav_msgs
 roscpp
 std_msgs
 message_generation
 sensor_msgs
)

add_service_files(
 FILES
 trackPos.srv
 serviceWayPoints.srv
)

generate_messages(
 DEPENDENCIES
 geometry_msgs
 nav_msgs
 std_msgs
)

catkin_package(
 CATKIN_DEPENDS 
 message_runtime 
 geometry_msgs 
 landshark_msgs
 tf
 actionlib
 landshark_common
 landshark_gps 
 nav_msgs 
 roscpp 
 std_msgs 
 sensor_msgs
)

include_directories(
 ${catkin_INCLUDE_DIRS}
)

install( PROGRAMS 
 src/track_pos.py
 nodes/vanilla_way_point_navigation.py
 DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)
=======
## Find catkin macros and libraries
## if COMPONENTS list like find_package(catkin REQUIRED COMPONENTS xyz)
## is used, also find other catkin packages
find_package(catkin REQUIRED COMPONENTS
  actionlib_msgs
  actionlib
  landshark_msgs
  landshark_common
  tf
  geometry_msgs
  landshark_gps
  nav_msgs
  roscpp
  std_msgs
  message_generation
  sensor_msgs
)

## Generate services in the 'srv' folder
 add_service_files(
   FILES
   trackPos.srv
 )

## Generate added messages and services with any dependencies listed here
 generate_messages(
   DEPENDENCIES
   geometry_msgs
   nav_msgs
   std_msgs
 )

###################################
## catkin specific configuration ##
###################################
catkin_package(
#  INCLUDE_DIRS include
#  LIBRARIES landshark_cmu_gps_drift
   CATKIN_DEPENDS 
   message_runtime 
   geometry_msgs 
   landshark_msgs
   tf
   actionlib
   landshark_common
   landshark_gps 
   nav_msgs 
   roscpp 
   std_msgs 
   sensor_msgs
#  DEPENDS system_lib
)

###########
## Build ##
###########
include_directories(
  ${catkin_INCLUDE_DIRS}
)

add_executable(talker src/talker.cpp)
target_link_libraries(talker ${catkin_LIBRARIES})
add_dependencies(talker landshark_cmu_track_pos_gencpp)
add_executable(listener src/listener.cpp)
target_link_libraries(listener ${catkin_LIBRARIES})
add_dependencies(listener landshark_cmu_track_pos_gencpp)
add_executable(listener2 src/listener2.cpp)
target_link_libraries(listener2 ${catkin_LIBRARIES})
add_dependencies(listener2 landshark_cmu_track_pos_gencpp)
add_executable(pubYaw src/pubYaw.cpp)
target_link_libraries(pubYaw ${catkin_LIBRARIES})
add_dependencies(pubYaw landshark_cmu_track_pos_gencpp)

#############
## Install ##
#############

install( PROGRAMS 
  src/track_pos.py
  src/talker.py
  src/listener.py
#  scripts/talker.py
#  scripts/listener.py
  DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
  )
>>>>>>> 00084a4747d782245904a4e8845cb15315e83c61


